language: cpp

compiler:
    - gcc

sudo: false

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test

addons:
    apt:
        packages:
            - g++-5

install:
    - export CXX="g++-4.8"

script:
    - $CXX -std=c++11 TwoSum/TwoSum.cpp -o TwoSum/TwoSum

branches:
    only:
        - master
