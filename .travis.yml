language: cpp

compiler:
    - gcc

before_install:
    - sudp apt-get update -qq

install:
    - sudo apt-get install -qq g++-4.8
    - export CXX="g++-4.8"

script:
    - $CXX -std=c++11 TwoSum/TwoSum.cpp -o TwoSum

branches:
    only:
        - master
